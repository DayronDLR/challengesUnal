<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.unal.tiendabd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiendaBd">
        <activity android:name=".ui.CRUD.CrudProduct" android:label="Modify Product"></activity>
        <activity android:name=".ui.CRUD.CrudCategory" android:label="Modify Category"/>
        <activity android:name=".ui.login.Login"  >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"

            android:theme="@style/Theme.TiendaBd.NoActionBar">

        </activity>
    </application>

</manifest>